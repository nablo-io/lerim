question: "What is our retrieval stack for semantic and lexical search?"
seed_records:
  - session_id: "seed-synthesis-1"
    kind: "decision"
    title: "Use sqlite-vec for semantic retrieval"
    body: "Semantic retrieval should use sqlite-vec instead of hash vectors."
    decision: "Use sqlite-vec for semantic retrieval."
    why: "This gives real local vector search."
  - session_id: "seed-synthesis-2"
    kind: "fact"
    title: "FTS5 remains the lexical retrieval layer"
    body: "FTS5 still handles lexical retrieval alongside the vector branch."
  - session_id: "seed-synthesis-3"
    kind: "constraint"
    title: "Embedding generation stays local"
    body: "Embedding generation should stay local through the ONNX pipeline instead of a hosted dependency."
expected:
  must_use_tools:
    - "search_context"
    - "get_context"
  must_not_use_tools: []
  answer_must_include_all:
    - "sqlite-vec"
    - "fts5"
    - "onnx"
