case: obsolete_low_value_durable_archived
summary: An old low-value non-episode durable record can be archived directly when it is not a duplicate worth superseding.

expected:
  must_use_events:
    - load_inventory
    - build_similarity_clusters
    - final_result
  must_not_use_events: []
