case: runtime_sync_then_maintain_then_ask_with_real_artifacts
expected:
  sync_workspace_subdir: "sync"
  maintain_workspace_subdir: "maintain"
  answer_must_include_all:
    - bounded backoff
    - worker retries
  answer_must_not_include:
    - runaway queue pressure
  ask_tool_call_order:
    - list_context
    - get_context
