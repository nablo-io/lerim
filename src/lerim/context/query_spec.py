"""Deterministic context query surface definitions."""

from __future__ import annotations

QUERY_ENTITIES = ("records", "versions", "sessions")
QUERY_MODES = ("list", "count")
QUERY_ORDER_FIELDS = ("created_at", "updated_at", "valid_from")
